// get data from viewpoints.json (onload?)

// function to populate cards with viewpoints

    // iterate through objects in viewpoints.json main array

        // const new_card = document.createElement('div');

        // newCard.setAttribute('class', ... all the bootstrap stuff)

        // insert subject, blurb, and icon into the card
